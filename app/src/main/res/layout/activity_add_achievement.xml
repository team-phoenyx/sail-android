<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_add_achievement"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="io.phoenyx.sail.AddAchievementActivity">

    <EditText
        android:id="@+id/achievementTitleEditText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:backgroundTint="@color/gray_highlight_color"
        android:ems="10"
        android:hint="@string/achievement_title_hint"
        android:inputType="textCapSentences|textAutoCorrect"
        android:maxLength="60"
        android:maxLines="1"
        android:textCursorDrawable="@drawable/color_cursor" />

    <TextView
        android:id="@+id/achievementDateTextView"
        style="@android:style/Widget.DeviceDefault.Light.Spinner"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_below="@+id/achievementTitleEditText"
        android:layout_marginStart="8dp"
        android:paddingTop="6dp"
        android:textAppearance="@style/TextAppearance.AppCompat"
        android:textSize="18sp" />

    <EditText
        android:id="@+id/achievementDescriptionEditText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:layout_alignParentStart="true"
        android:layout_below="@+id/achievementDateTextView"
        android:backgroundTint="@android:color/transparent"
        android:ems="10"
        android:gravity="top"
        android:hint="@string/description_hint"
        android:inputType="textMultiLine"
        android:maxLength="300"
        android:paddingEnd="8dp"
        android:paddingStart="8dp"
        android:textCursorDrawable="@drawable/color_cursor" />

</RelativeLayout>
